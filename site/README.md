# OOTODAY GitHub Pages

This folder hosts the static GitHub Pages site for OOTODAY.

It is intentionally a lightweight progress and landing page, not the full app runtime.
The main application depends on Next.js server features and Supabase auth, so GitHub Pages is only used here as a public-facing project page.
